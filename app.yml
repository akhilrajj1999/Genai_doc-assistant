command: ["streamlit", "run", "streamlit_app.py", "--server.port", "$DATABRICKS_APP_PORT", "--server.address", "0.0.0.0"]

env:
  - name: DATABRICKS_HOST
    value: "https://dbc-5e299c31-95bc.cloud.databricks.com"
  - name: DATABRICKS_TOKEN
    valueFrom:
      secretScope: "genai-doc-assistant"
      secretKey: "databricks-token"